schema_version = "1.0.0"

id = "blander_mcp"
version = "0.1.0"
name = "Blander MCP"
tagline = "AI animation factory MCP bridge for Blender"
maintainer = "yoyo"
type = "add-on"
blender_version_min = "4.2.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

tags = ["Development"]

[permissions]
network = "Runs a local TCP socket server for MCP client communication"


*****
Stack
*****

The list of constraints affecting an object or bone is called the *Constraint Stack*.
It's evaluated from top to bottom, and reordering it can drastically change the outcome.

.. figure:: /images/animation_constraints_interface_stack_example.png

Moving constraints is done by dragging their handle :bl-icon:`grip` with :kbd:`LMB`.
